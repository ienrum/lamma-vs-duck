import FenseWallScene from '@/src/widgets/fense-wall/ui/fense-wall-scene';

export const dynamic = 'force-dynamic';

const FenseWallPage = () => {
  return <FenseWallScene />;
};

export default FenseWallPage;
